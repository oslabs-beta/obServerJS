import React, { useContext } from 'react'
import {
  Button,
} from '@material-ui/core';

const MiddlewareFn = () => {

  return (
    <Button>MiddleWare Fn</Button>
  )
}

export default MiddlewareFn;